name: formgen_example
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2

  formgen_lib:
    path: ../formgen_lib

dev_dependencies:
  flutter_test:
    sdk: flutter

  formgen_generator:
    path: ../formgen_generator

  build_runner: ^1.3.5

flutter:
  uses-material-design: true
